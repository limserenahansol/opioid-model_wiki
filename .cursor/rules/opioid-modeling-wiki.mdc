---
description: Addiction Motivational Model (AMM) — latent state PR modeling
globs: opioid_behavioral_model/**/*
alwaysApply: false
---

# Addiction Motivational Model (AMM)

Read `model/07_DATA_RULES_AND_LIKELIHOOD.md` and `model/08_FITTING_PRIORITY.md` before fitting code.

- Canonical spec: `model/` (logic, states, M0–M4, workflow).
- run_009: day_index >= 4; passive During 6–10 mask licks (unobserved), keep rewards; PairID RE.
- Tier 1 now: M0 drift + M1 softplus(x); Tier 3–4 conditional on identifiability.
- Do not add email logs. Do not label passive model "PIT" — use generalized C×G.
- `x_t` hidden; PR = continue/pause/quit. Data: `docs/DATA_SOURCES.md`.
